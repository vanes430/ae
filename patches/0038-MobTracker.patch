From: Folia Compatibility Patch
Subject: [PATCH 0038] MobTracker: 

AE PATCH REASON: 
AE PATCH FIX: 
AE PATCH RISK: 

---
diff --git a/net/advancedplugins/ae/items/item/MobTracker.java b/net/advancedplugins/ae/items/item/MobTracker.java
--- a/net/advancedplugins/ae/items/item/MobTracker.java	2026-07-30 10:27:31.685350276 +0000
+++ b/net/advancedplugins/ae/items/item/MobTracker.java	2026-07-30 12:30:22.747766032 +0000
@@ -5,9 +5,10 @@
 import java.util.List;
 import net.advancedplugins.ae.api.ItemApplyEvent;
 import net.advancedplugins.ae.impl.effects.effects.actions.utils.ItemInHand;
+import net.advancedplugins.ae.Core;
 import net.advancedplugins.ae.impl.utils.ColorUtils;
-import net.advancedplugins.ae.impl.utils.SchedulerUtils;
 import net.advancedplugins.ae.impl.utils.items.ItemBuilder;
+import io.papermc.paper.threadedregions.scheduler.ScheduledTask;
 import net.advancedplugins.ae.impl.utils.items.ItemFlagFix;
 import net.advancedplugins.ae.items.AEItem;
 import net.advancedplugins.ae.utils.AManager;
@@ -78,7 +79,7 @@
 
                assert var9 != null;
 
-               var9.add(var8);
+               ((List<String>)var9).add(var8);
                var3.setLore((List)var9);
                var2.setItemMeta(var3);
                var2 = NBTapi.addNBTTag("mobtrak", var7 + "", var2);
@@ -96,37 +97,37 @@
    }
 
    @Override
-   public void onPlayerKillEntity(EntityDeathEvent var1) {
-      if (!(var1.getEntity() instanceof Player)) {
-         Player var2 = var1.getEntity().getKiller();
-         ItemInHand var3 = new ItemInHand(var2);
-         if (AManager.isValid(var3.get())) {
-            if (NBTapi.contains("mobtrak", var3.get())) {
-               SchedulerUtils.runTaskLater(() -> {
-                  String var2x = NBTapi.get("mobtrak", var3.get());
-                  if (var2x != null) {
-                     int var3x = Integer.parseInt(var2x);
-                     int var4 = var3x + 1;
-                     ItemStack var5 = var3.get();
-                     ItemMeta var6 = var5.getItemMeta();
-                     ArrayList var7 = new ArrayList();
-                     String var8 = ColorUtils.stripColor(this.getNewAppliedLoreString()).replace("%stats%", var3x + "");
-                     String var9 = this.getNewAppliedLoreString().replace("%stats%", var4 + "");
-                     if (var6.hasLore()) {
-                        for (String var11 : var6.getLore()) {
-                           if (!ColorUtils.stripColor(var11).equals(var8)) {
-                              var7.add(var11);
+   public void onPlayerKillEntity(EntityDeathEvent event) {
+      if (!(event.getEntity() instanceof Player)) {
+         Player player = event.getEntity().getKiller();
+         ItemInHand itemInHand = new ItemInHand(player);
+         if (AManager.isValid(itemInHand.get())) {
+            if (NBTapi.contains("mobtrak", itemInHand.get())) {
+               player.getScheduler().runDelayed(Core.getInstance(), task -> {
+                  String killsStr = NBTapi.get("mobtrak", itemInHand.get());
+                  if (killsStr != null) {
+                     int currentKills = Integer.parseInt(killsStr);
+                     int newKills = currentKills + 1;
+                     ItemStack item = itemInHand.get();
+                     ItemMeta meta = item.getItemMeta();
+                     ArrayList lore = new ArrayList();
+                     String oldLore = ColorUtils.stripColor(this.getNewAppliedLoreString()).replace("%stats%", currentKills + "");
+                     String newLoreLine = this.getNewAppliedLoreString().replace("%stats%", newKills + "");
+                     if (meta.hasLore()) {
+                        for (String line : meta.getLore()) {
+                           if (!ColorUtils.stripColor(line).equals(oldLore)) {
+                              lore.add(line);
                            }
                         }
                      }
 
-                     var7.add(var9);
-                     var6.setLore(var7);
-                     var5.setItemMeta(var6);
-                     ItemStack var12 = NBTapi.addNBTTag("mobtrak", var4 + "", var5);
-                     var3.set(var12);
+                     lore.add(newLoreLine);
+                     meta.setLore(lore);
+                     item.setItemMeta(meta);
+                     ItemStack updated = NBTapi.addNBTTag("mobtrak", newKills + "", item);
+                     itemInHand.set(updated);
                   }
-               });
+               }, null, 1L);
             }
          }
       }
