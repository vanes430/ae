From: Folia Compatibility Patch
Subject: [PATCH 0010] UpdateChecker: Migrate to GlobalRegionScheduler and AsyncScheduler

AE PATCH REASON: Bukkit scheduler incompatible with Folia plugin lifecycle
AE PATCH FIX: Migrate to GlobalRegionScheduler and AsyncScheduler
AE PATCH RISK: LOW
AE PATCH EXCLUDES: net/advancedplugins/ae/impl/utils/plugin/UpdateChecker.class, net/advancedplugins/ae/impl/utils/plugin/UpdateChecker$*.class

---
diff --git a/net/advancedplugins/ae/impl/utils/plugin/UpdateChecker.java b/net/advancedplugins/ae/impl/utils/plugin/UpdateChecker.java
index 6b725de..fb1e6ad 100644
--- a/net/advancedplugins/ae/impl/utils/plugin/UpdateChecker.java
+++ b/net/advancedplugins/ae/impl/utils/plugin/UpdateChecker.java
@@ -1,9 +1,6 @@
 package net.advancedplugins.ae.impl.utils.plugin;
 
-import java.util.concurrent.CompletableFuture;
 import net.advancedplugins.ae.impl.utils.ASManager;
-import net.advancedplugins.ae.impl.utils.FoliaScheduler;
-import net.advancedplugins.ae.impl.utils.trycatch.TryCatchUtil;
 import org.bukkit.Bukkit;
 import org.bukkit.ChatColor;
 import org.bukkit.plugin.java.JavaPlugin;
@@ -11,57 +8,60 @@ import org.bukkit.plugin.java.JavaPlugin;
 public class UpdateChecker {
    private static final String UPDATE_URL = "https://advancedplugins.net/api/v1/getVersion.php?plugin=";
 
-   public static void checkUpdate(JavaPlugin var0) {
-      FoliaScheduler.runTaskLaterAsynchronously(
-         var0,
-         () -> {
-            String var1 = var0.getDescription().getVersion();
-            String var2 = var0.getDescription().getName();
-            CompletableFuture.<String>supplyAsync(
-                  () -> (String)TryCatchUtil.tryAndReturn(() -> ASManager.fetchJsonFromUrl("https://advancedplugins.net/api/v1/getVersion.php?plugin=" + var2))
-               )
-               .thenAccept(
-                  var2x -> {
-                     if (!var2x.isEmpty() && !isLatest(var1, var2x)) {
-                        Bukkit.getConsoleSender()
-                           .sendMessage(
-                              ChatColor.translateAlternateColorCodes(
-                                 '&', "&7[" + var2 + "] &eYou're using an outdated version of " + var2 + ". A new version is available: &f" + var2x
-                              )
-                           );
-                        Bukkit.getConsoleSender()
-                           .sendMessage(
-                              ChatColor.translateAlternateColorCodes(
-                                 '&',
-                                 "&7["
-                                    + var2
-                                    + "] &7Keep your Advanced plugins up to date automatically with &bMintServers&7 Unlimited Hosting: &bhttps://mintservers.com/"
-                              )
-                           );
-                     }
-                  }
-               );
+   public static void checkUpdate(JavaPlugin plugin) {
+      Bukkit.getAsyncScheduler().runDelayed(
+         plugin,
+         task -> {
+            String currentVersion = plugin.getDescription().getVersion();
+            String pluginName = plugin.getDescription().getName();
+
+            String latestVersion;
+            try {
+               latestVersion = ASManager.fetchJsonFromUrl("https://advancedplugins.net/api/v1/getVersion.php?plugin=" + pluginName);
+            } catch (Exception var5) {
+               var5.printStackTrace();
+               return;
+            }
+
+            if (!latestVersion.isEmpty() && !isLatest(currentVersion, latestVersion)) {
+               Bukkit.getConsoleSender()
+                  .sendMessage(
+                     ChatColor.translateAlternateColorCodes(
+                        '&', "&7[" + pluginName + "] &eYou're using an outdated version of " + pluginName + ". A new version is available: &f" + latestVersion
+                     )
+                  );
+               Bukkit.getConsoleSender()
+                  .sendMessage(
+                     ChatColor.translateAlternateColorCodes(
+                        '&',
+                        "&7["
+                           + pluginName
+                           + "] &7Keep your Advanced plugins up to date automatically with &bMintServers&7 Unlimited Hosting: &bhttps://mintservers.com/"
+                     )
+                  );
+            }
          },
-         20L
+         1000L,
+         java.util.concurrent.TimeUnit.MILLISECONDS
       );
    }
 
-   private static boolean isLatest(String var0, String var1) {
-      String[] var2 = var0.split("\\.");
-      String[] var3 = var1.split("\\.");
+   private static boolean isLatest(String currentVersion, String latestVersion) {
+      String[] currentVersionParts = currentVersion.split("\\.");
+      String[] latestVersionParts = latestVersion.split("\\.");
 
-      for (int var4 = 0; var4 < Math.min(var2.length, var3.length); var4++) {
-         int var5 = Integer.parseInt(var2[var4]);
-         int var6 = Integer.parseInt(var3[var4]);
-         if (var5 < var6) {
+      for (int i = 0; i < Math.min(currentVersionParts.length, latestVersionParts.length); i++) {
+         int currentPart = Integer.parseInt(currentVersionParts[i]);
+         int latestPart = Integer.parseInt(latestVersionParts[i]);
+         if (currentPart < latestPart) {
             return false;
          }
 
-         if (var5 > var6) {
+         if (currentPart > latestPart) {
             return true;
          }
       }
 
-      return var2.length >= var3.length;
+      return currentVersionParts.length >= latestVersionParts.length;
    }
 }

