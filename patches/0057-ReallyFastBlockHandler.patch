From: Shimazu <vanessimbolon2020@gmail.com>
Date: Fri, 31 Jul 2026 02:19:04 +0000
Subject: ReallyFastBlockHandler: Thread safety and native Folia scheduling

AE PATCH REASON: HashMap is not thread-safe for region-thread access; FoliaScheduler delegation is no longer needed after direct native scheduler migration.
AE PATCH FIX: Replace HashMap with ConcurrentHashMap for handlers map. Replace FoliaScheduler.runTask() with native Bukkit.getRegionScheduler().execute() and Bukkit.getGlobalRegionScheduler().execute() calls. Rename var1/var2/var3 to descriptive names.
AE PATCH RISK: LOW

---
diff --git a/net/advancedplugins/ae/impl/utils/ReallyFastBlockHandler.java b/net/advancedplugins/ae/impl/utils/ReallyFastBlockHandler.java
--- a/net/advancedplugins/ae/impl/utils/ReallyFastBlockHandler.java	2026-07-30 10:27:31.675350186 +0000
+++ b/net/advancedplugins/ae/impl/utils/ReallyFastBlockHandler.java	2026-07-30 10:28:05.912658843 +0000
@@ -1,7 +1,7 @@
 package net.advancedplugins.ae.impl.utils;
 
-import java.util.HashMap;
 import java.util.Map;
+import java.util.concurrent.ConcurrentHashMap;
 import net.advancedplugins.ae.impl.utils.nbt.utils.MinecraftVersion;
 import org.bukkit.Bukkit;
 import org.bukkit.Material;
@@ -11,44 +11,48 @@
 import org.bukkit.block.data.Waterlogged;
 
 public class ReallyFastBlockHandler {
-   private static final Map<World, ReallyFastBlockHandler> handlers = new HashMap<>();
+   private static final Map<World, ReallyFastBlockHandler> handlers = new ConcurrentHashMap<>();
    private final World world;
 
-   public static ReallyFastBlockHandler getForWorld(World var0) {
-      return handlers.computeIfAbsent(var0, ReallyFastBlockHandler::new);
+   public static ReallyFastBlockHandler getForWorld(World world) {
+      return handlers.computeIfAbsent(world, ReallyFastBlockHandler::new);
    }
 
-   public ReallyFastBlockHandler(World var1) {
-      this.world = var1;
+   public ReallyFastBlockHandler(World world) {
+      this.world = world;
    }
 
-   public void setType(Material var1, Block... var2) {
+   public void setType(Material material, Block... blocks) {
       if (!Bukkit.isPrimaryThread()) {
-         FoliaScheduler.runTask(ASManager.getInstance(), () -> this.setType(var1, var2));
+         if (blocks.length > 0) {
+            Bukkit.getRegionScheduler().execute(ASManager.getInstance(), blocks[0].getLocation(), () -> this.setType(material, blocks));
+         } else {
+            Bukkit.getGlobalRegionScheduler().execute(ASManager.getInstance(), () -> this.setType(material, blocks));
+         }
       } else {
-         for (Block var6 : var2) {
-            if (var6.getType() != var1) {
-               boolean var7 = false;
-               int var8 = 0;
-               if (var6.getBlockData() instanceof Waterlogged var9) {
-                  var7 = var9.isWaterlogged();
+         for (Block block : blocks) {
+            if (block.getType() != material) {
+               boolean waterLogged = false;
+               int waterLevel = 0;
+               if (block.getBlockData() instanceof Waterlogged waterlogged) {
+                  waterLogged = waterlogged.isWaterlogged();
                }
 
                if (MinecraftVersion.isPaper()) {
-                  String var11 = var6.getBlockData().getMaterial().name();
-                  if (var11.equals("SEA_PICKLE") || var11.contains("CORAL") || var11.contains("KELP") || var11.contains("SEAGRASS")) {
-                     var8 = var6.getWorld().getFluidData(var6.getLocation()).getLevel();
+                  String name = block.getBlockData().getMaterial().name();
+                  if (name.equals("SEA_PICKLE") || name.contains("CORAL") || name.contains("KELP") || name.contains("SEAGRASS")) {
+                     waterLevel = block.getWorld().getFluidData(block.getLocation()).getLevel();
                   }
                }
 
-               var6.setType(var1);
-               if (var7 || var8 > 0) {
-                  var6.setType(Material.WATER);
+               block.setType(material);
+               if (waterLogged || waterLevel > 0) {
+                  block.setType(Material.WATER);
                }
 
-               if (var8 > 0 && var6.getBlockData() instanceof Levelled var12) {
-                  var12.setLevel(var8);
-                  var6.setBlockData(var12);
+               if (waterLevel > 0 && block.getBlockData() instanceof Levelled levelled) {
+                  levelled.setLevel(waterLevel);
+                  block.setBlockData(levelled);
                }
             }
          }
