From: Folia Compatibility Patch
Subject: [PATCH 0009] AddWalkSpeedEffect: 

AE PATCH REASON: 
AE PATCH FIX: 
AE PATCH RISK: 

---
diff --git a/net/advancedplugins/ae/impl/effects/effects/effects/internal/AddWalkSpeedEffect.java b/net/advancedplugins/ae/impl/effects/effects/effects/internal/AddWalkSpeedEffect.java
--- a/net/advancedplugins/ae/impl/effects/effects/effects/internal/AddWalkSpeedEffect.java	2026-07-30 10:27:31.664350087 +0000
+++ b/net/advancedplugins/ae/impl/effects/effects/effects/internal/AddWalkSpeedEffect.java	2026-07-30 12:21:00.821784903 +0000
@@ -1,13 +1,13 @@
 package net.advancedplugins.ae.impl.effects.effects.effects.internal;
 
+import io.papermc.paper.threadedregions.scheduler.ScheduledTask;
 import java.util.HashMap;
 import java.util.Map;
 import java.util.UUID;
+import net.advancedplugins.ae.impl.effects.effects.EffectsHandler;
 import net.advancedplugins.ae.impl.effects.effects.actions.execution.ExecutionTask;
 import net.advancedplugins.ae.impl.effects.effects.effects.AdvancedEffect;
 import net.advancedplugins.ae.impl.utils.ASManager;
-import net.advancedplugins.ae.impl.utils.SchedulerUtils;
-import org.bukkit.Bukkit;
 import org.bukkit.entity.LivingEntity;
 import org.bukkit.entity.Player;
 import org.bukkit.event.EventHandler;
@@ -17,7 +17,7 @@
 
 public class AddWalkSpeedEffect extends AdvancedEffect {
    private final Map<UUID, Long> speedDuration = new HashMap<>();
-   private final Map<UUID, Integer> removalTasks = new HashMap<>();
+   private final Map<UUID, ScheduledTask> removalTasks = new HashMap<>();
 
    public AddWalkSpeedEffect(JavaPlugin var1) {
       super(var1, "ADD_WALK_SPEED", "Adds to the players walk speed (from -1 to 1) without applying potion", "%e:<SPEED>:[TICKS]");
@@ -26,45 +26,45 @@
    }
 
    @Override
-   public boolean executeEffect(ExecutionTask var1, LivingEntity var2, String[] var3) {
-      if (!(var2 instanceof Player)) {
+   public boolean executeEffect(ExecutionTask task, LivingEntity entity, String[] args) {
+      if (!(entity instanceof Player)) {
          return false;
-      } else if (var3.length < 1) {
+      } else if (args.length < 1) {
          return false;
       } else {
-         Player var4 = (Player)var2;
-         float var5 = Float.parseFloat(var3[0]);
-         if (var1.getBuilder().isPermanent() && var3.length == 1) {
-            if (var1.getBuilder().isRemoved()) {
-               var4.setWalkSpeed(0.2F);
+         Player player = (Player)entity;
+         float speed = Float.parseFloat(args[0]);
+         if (task.getBuilder().isPermanent() && args.length == 1) {
+            if (task.getBuilder().isRemoved()) {
+               player.setWalkSpeed(0.2F);
             } else {
-               this.addSpeed(var4, var5);
+               this.addSpeed(player, speed);
             }
 
             return true;
-         } else if (var3.length >= 2) {
-            if (var1.getBuilder().isPermanent() && var1.getBuilder().isRemoved()) {
-               var4.setWalkSpeed(0.2F);
+         } else if (args.length >= 2) {
+            if (task.getBuilder().isPermanent() && task.getBuilder().isRemoved()) {
+               player.setWalkSpeed(0.2F);
                return true;
             } else {
-               long var6 = (long)ASManager.parseInt(var3[1]) / 20L * 1000L;
-               this.speedDuration.put(var4.getUniqueId(), System.currentTimeMillis() + var6);
-               UUID var8 = var4.getUniqueId();
-               if (this.removalTasks.containsKey(var8)) {
-                  int var9 = this.removalTasks.remove(var8);
-                  Bukkit.getScheduler().cancelTask(var9);
+               long durationMs = (long)ASManager.parseInt(args[1]) / 20L * 1000L;
+               this.speedDuration.put(player.getUniqueId(), System.currentTimeMillis() + durationMs);
+               UUID uuid = player.getUniqueId();
+               ScheduledTask oldTask = this.removalTasks.remove(uuid);
+               if (oldTask != null) {
+                  oldTask.cancel();
                }
 
-               this.removalTasks.put(var8, SchedulerUtils.runTaskLater(() -> {
-                  this.speedDuration.remove(var8);
-                  var4.setWalkSpeed(0.2F);
-                  this.removalTasks.remove(var8);
-               }, var6 / 50L));
-               this.addSpeed(var4, var5);
+               this.removalTasks.put(uuid, player.getScheduler().runDelayed(EffectsHandler.getInstance(), scheduledTask -> {
+                  this.speedDuration.remove(uuid);
+                  player.setWalkSpeed(0.2F);
+                  this.removalTasks.remove(uuid);
+               }, null, durationMs / 50L));
+               this.addSpeed(player, speed);
                return true;
             }
          } else {
-            this.addSpeed(var4, var5);
+            this.addSpeed(player, speed);
             return true;
          }
       }
@@ -73,17 +73,17 @@
    @EventHandler(
       priority = EventPriority.HIGHEST
    )
-   public void onPlayerQuit(PlayerQuitEvent var1) {
-      Player var2 = var1.getPlayer();
-      UUID var3 = var2.getUniqueId();
-      if (this.speedDuration.containsKey(var3)) {
-         this.speedDuration.remove(var3);
-         var2.setWalkSpeed(0.2F);
+   public void onPlayerQuit(PlayerQuitEvent event) {
+      Player player = event.getPlayer();
+      UUID uuid = player.getUniqueId();
+      if (this.speedDuration.containsKey(uuid)) {
+         this.speedDuration.remove(uuid);
+         player.setWalkSpeed(0.2F);
       }
 
-      if (this.removalTasks.containsKey(var3)) {
-         int var4 = this.removalTasks.remove(var3);
-         Bukkit.getScheduler().cancelTask(var4);
+      ScheduledTask scheduledTask = this.removalTasks.remove(uuid);
+      if (scheduledTask != null) {
+         scheduledTask.cancel();
       }
    }
 
