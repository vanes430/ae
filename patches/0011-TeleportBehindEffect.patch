From: Folia Compatibility Patch
Subject: [PATCH 0011] TeleportBehindEffect: Rename variables and use teleportAsync

AE PATCH REASON: Obfuscated variable names hinder maintainability.
AE PATCH FIX: Rename var1/var2/var3 to descriptive names. Replace teleport() with teleportAsync() for compatibility.
AE PATCH RISK: LOW

---
diff --git a/net/advancedplugins/ae/impl/effects/effects/effects/internal/TeleportBehindEffect.java b/net/advancedplugins/ae/impl/effects/effects/effects/internal/TeleportBehindEffect.java
--- a/net/advancedplugins/ae/impl/effects/effects/effects/internal/TeleportBehindEffect.java	2026-07-30 10:27:31.665350096 +0000
+++ b/net/advancedplugins/ae/impl/effects/effects/effects/internal/TeleportBehindEffect.java	2026-07-30 10:28:05.874658500 +0000
@@ -9,26 +9,26 @@
 import org.bukkit.plugin.java.JavaPlugin;
 
 public class TeleportBehindEffect extends AdvancedEffect {
-   public TeleportBehindEffect(JavaPlugin var1) {
-      super(var1, "TELEPORT_BEHIND", "Teleport behind other entity", "%e");
+   public TeleportBehindEffect(JavaPlugin plugin) {
+      super(plugin, "TELEPORT_BEHIND", "Teleport behind other entity", "%e");
    }
 
    @Override
-   public boolean executeEffect(ExecutionTask var1, LivingEntity var2, String[] var3) {
-      LivingEntity var4 = this.getOtherEntity(var2, var1);
-      Location var5 = var4.getLocation();
-      float var6 = var5.getYaw() + 90.0F;
-      if (var6 < 0.0F) {
-         var6 += 360.0F;
+   public boolean executeEffect(ExecutionTask task, LivingEntity target, String[] args) {
+      LivingEntity nonTarget = this.getOtherEntity(target, task);
+      Location from = nonTarget.getLocation();
+      float nang = from.getYaw() + 90.0F;
+      if (nang < 0.0F) {
+         nang += 360.0F;
       }
 
-      double var7 = Math.cos(Math.toRadians((double)var6));
-      double var9 = Math.sin(Math.toRadians((double)var6));
-      Location var11 = new Location(var5.getWorld(), var5.getX() - var7, var5.getY(), var5.getZ() - var9, var5.getYaw(), var5.getPitch());
-      if (!ASManager.isAir(var11.getBlock())) {
+      double nX = Math.cos(Math.toRadians(nang));
+      double nZ = Math.sin(Math.toRadians(nang));
+      Location to = new Location(from.getWorld(), from.getX() - nX, from.getY(), from.getZ() - nZ, from.getYaw(), from.getPitch());
+      if (!ASManager.isAir(to.getBlock())) {
          return true;
       } else {
-         var2.teleport(var11, TeleportCause.UNKNOWN);
+         target.teleportAsync(to, TeleportCause.UNKNOWN);
          return true;
       }
    }
