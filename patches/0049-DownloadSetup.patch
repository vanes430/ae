From: Folia Compatibility Patch
Subject: [PATCH 0042] DownloadSetup: 

AE PATCH REASON: 
AE PATCH FIX: 
AE PATCH RISK: 

---
diff --git a/net/advancedplugins/ae/handlers/netbackend/DownloadSetup.java b/net/advancedplugins/ae/handlers/netbackend/DownloadSetup.java
--- a/net/advancedplugins/ae/handlers/netbackend/DownloadSetup.java	2026-07-30 10:27:31.650349961 +0000
+++ b/net/advancedplugins/ae/handlers/netbackend/DownloadSetup.java	2026-07-30 12:31:48.700529998 +0000
@@ -4,78 +4,76 @@
 import java.io.IOException;
 import java.net.URL;
 import java.net.URLConnection;
+import io.papermc.paper.threadedregions.scheduler.ScheduledTask;
 import net.advancedplugins.ae.Core;
 import net.advancedplugins.ae.handlers.commands.MCI;
-import net.advancedplugins.ae.impl.utils.SchedulerUtils;
+import net.advancedplugins.ae.impl.effects.effects.EffectsHandler;
 import net.advancedplugins.ae.impl.utils.nbt.utils.MinecraftVersion;
 import net.advancedplugins.ae.utils.AManager;
 import net.advancedplugins.ae.utils.files.FileUtilsCopyStream;
-import org.apache.commons.io.FileUtils;
+import java.nio.file.Files;
+import java.nio.file.StandardCopyOption;
 import org.bukkit.Bukkit;
 
 class DownloadSetup {
-   public static void init(Setup var0) {
-      if (Bukkit.isPrimaryThread()) {
-         SchedulerUtils.runTaskAsync(() -> init(var0));
-      } else {
-         File var1 = Core.getInstance().getDataFolder();
-         if (var0 == Setup.DEFAULT) {
-            for (File var5 : var1.listFiles()) {
-               var5.delete();
-            }
-         } else {
-            String[] var12 = var0.getFiles();
-            String var13 = Setup.getLink(var0);
-
-            for (String var7 : var12) {
-               File var8 = new File(var1, var7);
-               if (var8.exists()) {
-                  var8.delete();
-               }
-
-               try {
-                  URL var9 = new URL(var13 + var7);
-                  URLConnection var10 = var9.openConnection();
-                  var10.setRequestProperty("User-Agent", "Mozilla/5.0 (Windows NT 6.1; WOW64; rv:31.0) Gecko/20100101 Firefox/31.0");
-                  var10.connect();
-                  FileUtilsCopyStream.copyInputStreamToFile(var10.getInputStream(), var8);
-                  AManager.error(MCI.color("&7Downloading &a" + var7 + " &7file..."));
-               } catch (IOException var11) {
-                  var11.printStackTrace();
-                  AManager.error(MCI.color("&cFailed to download &a" + var7 + " &7file..."));
-               }
-            }
-         }
-
-         AManager.error(MCI.color("&a-----------------------------------------"));
-         AManager.error(MCI.color("&7Download has been &acompleted!"));
-         AManager.error(MCI.color("&a&nTo install&7 these new files, restart your server."));
-         AManager.error(MCI.color("&a-----------------------------------------"));
-      }
-   }
-
-   private static void rename(File var0) {
-      File var1 = new File(var0, "config.yml");
-      File var2 = new File(var0, "enchantments.yml");
-      File var3 = new File(var0, "menus/tinkerer.yml");
-      File var4 = new File(var0, "groups.yml");
-      String var5 = System.currentTimeMillis() + "";
-
-      try {
-         copyFile(var1, new File(var0, var5 + "-configBackup.yml"));
-         copyFile(var2, new File(var0, var5 + "-enchantmentsBackup.yml"));
-         copyFile(var3, new File(var0, var5 + "-tinkererBackup.yml"));
-         copyFile(var4, new File(var0, var5 + "-groupsBackup.yml"));
-      } catch (IOException var7) {
-         var7.printStackTrace();
-      }
-   }
-
-   private static void copyFile(File var0, File var1) {
-      if (!MinecraftVersion.isNew()) {
-         FileUtils.copyFile(var0, var1);
-      } else {
-         FileUtils.copyFile(var0, var1);
-      }
-   }
+   public static void init(Setup setup) {
+       if (Bukkit.isPrimaryThread()) {
+          Bukkit.getAsyncScheduler().runNow(EffectsHandler.getInstance(), task -> init(setup));
+       } else {
+          File dataFolder = Core.getInstance().getDataFolder();
+          if (setup == Setup.DEFAULT) {
+             for (File file : dataFolder.listFiles()) {
+                file.delete();
+             }
+          } else {
+             String[] files = setup.getFiles();
+             String baseUrl = Setup.getLink(setup);
+
+             for (String fileName : files) {
+                File targetFile = new File(dataFolder, fileName);
+                if (targetFile.exists()) {
+                   targetFile.delete();
+                }
+
+                try {
+                   URL url = new URL(baseUrl + fileName);
+                   URLConnection connection = url.openConnection();
+                   connection.setRequestProperty("User-Agent", "Mozilla/5.0 (Windows NT 6.1; WOW64; rv:31.0) Gecko/20100101 Firefox/31.0");
+                   connection.connect();
+                   FileUtilsCopyStream.copyInputStreamToFile(connection.getInputStream(), targetFile);
+                   AManager.error(MCI.color("&7Downloading &a" + fileName + " &7file..."));
+                } catch (IOException e) {
+                   e.printStackTrace();
+                   AManager.error(MCI.color("&cFailed to download &a" + fileName + " &7file..."));
+                }
+             }
+          }
+
+          AManager.error(MCI.color("&a-----------------------------------------"));
+          AManager.error(MCI.color("&7Download has been &acompleted!"));
+          AManager.error(MCI.color("&a&nTo install&7 these new files, restart your server."));
+          AManager.error(MCI.color("&a-----------------------------------------"));
+       }
+    }
+
+    private static void rename(File dir) {
+       File configFile = new File(dir, "config.yml");
+       File enchantsFile = new File(dir, "enchantments.yml");
+       File tinkererFile = new File(dir, "menus/tinkerer.yml");
+       File groupsFile = new File(dir, "groups.yml");
+       String timestamp = System.currentTimeMillis() + "";
+
+       try {
+          copyFile(configFile, new File(dir, timestamp + "-configBackup.yml"));
+          copyFile(enchantsFile, new File(dir, timestamp + "-enchantmentsBackup.yml"));
+          copyFile(tinkererFile, new File(dir, timestamp + "-tinkererBackup.yml"));
+          copyFile(groupsFile, new File(dir, timestamp + "-groupsBackup.yml"));
+       } catch (IOException e) {
+          e.printStackTrace();
+       }
+    }
+
+    private static void copyFile(File source, File target) throws IOException {
+        Files.copy(source.toPath(), target.toPath(), StandardCopyOption.REPLACE_EXISTING);
+}
 }
