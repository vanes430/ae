From: Folia Compatibility Patch
Subject: [PATCH 0015] GuardEffect: Migrate potion, boost, extinguish, guard, and teleport effects to SchedulerUtils/FoliaScheduler

AE PATCH REASON: Standard enchantment effects use legacy scheduler
AE PATCH FIX: Migrate potion, boost, extinguish, guard, and teleport effects to SchedulerUtils/FoliaScheduler
AE PATCH RISK: MEDIUM
AE PATCH EXCLUDES: net/advancedplugins/ae/impl/effects/effects/effects/internal/GuardEffect.class, net/advancedplugins/ae/impl/effects/effects/effects/internal/GuardEffect$*.class

---
diff --git a/net/advancedplugins/ae/impl/effects/effects/effects/internal/GuardEffect.java b/net/advancedplugins/ae/impl/effects/effects/effects/internal/GuardEffect.java
index 20361b3..ef08f59 100644
--- a/net/advancedplugins/ae/impl/effects/effects/effects/internal/GuardEffect.java
+++ b/net/advancedplugins/ae/impl/effects/effects/effects/internal/GuardEffect.java
@@ -5,8 +5,8 @@ import java.util.Locale;
 import java.util.Map;
 import java.util.Objects;
 import java.util.UUID;
+import java.util.concurrent.ConcurrentHashMap;
 import java.util.Map.Entry;
-import lombok.Generated;
 import net.advancedplugins.ae.impl.effects.effects.EffectsHandler;
 import net.advancedplugins.ae.impl.effects.effects.actions.execution.ExecutionTask;
 import net.advancedplugins.ae.impl.effects.effects.effects.AdvancedEffect;
@@ -31,11 +31,11 @@ import org.bukkit.potion.PotionEffect;
 import org.bukkit.potion.PotionEffectType;
 
 public class GuardEffect extends AdvancedEffect {
-   private final Map<UUID, UUID> guards = new HashMap<>();
+   private final Map<UUID, UUID> guards = new ConcurrentHashMap<>();
    private static GuardEffect guardEffect;
 
-   public GuardEffect(JavaPlugin var1) {
-      super(var1, "GUARD", "Spawn a mob guarding player", "%e:<ENTITY>:<SECONDS>:<AMOUNT:><NAME>:<SWITCH_ROLES>");
+   public GuardEffect(JavaPlugin plugin) {
+      super(plugin, "GUARD", "Spawn a mob guarding player", "%e:<ENTITY>:<SECONDS>:<AMOUNT:><NAME>:<SWITCH_ROLES>");
       this.addArgument(0, EntityType.class);
       this.addArgument(1, Integer.class);
       this.addArgument(2, Integer.class);
@@ -44,145 +44,145 @@ public class GuardEffect extends AdvancedEffect {
       guardEffect = this;
    }
 
-   public boolean executeEffect(ExecutionTask var1, LivingEntity var2, String[] var3) {
-      String var4 = var3[0].toUpperCase(Locale.ROOT);
-      boolean var5 = var4.startsWith("BABY_");
-      String var6 = var5 ? var4.substring("BABY_".length()) : var4;
-      if (!ASManager.isValidEnum(EntityType.class, var6)) {
-         EffectsHandler.getInstance().getLogger().warning("Tried to summon GUARD of invalid entity type: \"" + var6 + "\".");
+   public boolean executeEffect(ExecutionTask task, LivingEntity target, String[] args) {
+      String type = args[0].toUpperCase(Locale.ROOT);
+      boolean isBaby = type.startsWith("BABY_");
+      String typeCheck = isBaby ? type.substring("BABY_".length()) : type;
+      if (!ASManager.isValidEnum(EntityType.class, typeCheck)) {
+         EffectsHandler.getInstance().getLogger().warning("Tried to summon GUARD of invalid entity type: \"" + typeCheck + "\".");
          return false;
       } else {
-         EntityType var7 = EntityType.valueOf(var6);
-         int var8 = 160;
-         int var9 = 1;
-         if (var3.length > 1 && MathUtils.isInteger(var3[1])) {
-            var8 = ASManager.parseInt(var3[1]) * 20;
+         EntityType entityType = EntityType.valueOf(typeCheck);
+         int liveTicks = 160;
+         int amount = 1;
+         if (args.length > 1 && MathUtils.isInteger(args[1])) {
+            liveTicks = ASManager.parseInt(args[1]) * 20;
          }
 
-         if (var3.length > 2 && MathUtils.isInteger(var3[2].trim())) {
-            var9 = ASManager.parseInt(var3[2].trim());
+         if (args.length > 2 && MathUtils.isInteger(args[2].trim())) {
+            amount = ASManager.parseInt(args[2].trim());
          }
 
-         boolean var10 = false;
-         if (var3.length > 4) {
-            var10 = ASManager.parseBoolean(var3[4], false);
+         boolean switchRoles = false;
+         if (args.length > 4) {
+            switchRoles = ASManager.parseBoolean(args[4], false);
          }
 
-         LivingEntity var11 = this.getOtherEntity(var2, var1);
-         LivingEntity var12 = var2;
-         if (var10) {
-            var12 = var11;
-            var11 = var2;
+         LivingEntity otherEntity = this.getOtherEntity(target, task);
+         LivingEntity finalTarget = target;
+         if (switchRoles) {
+            finalTarget = otherEntity;
+            otherEntity = target;
          }
 
-         String var13 = "";
-         if (var3.length > 3) {
-            var13 = ColorUtils.format(var3[3].replaceAll("%player name%", var12.getName()));
+         String name = "";
+         if (args.length > 3) {
+            name = ColorUtils.format(args[3].replaceAll("%player name%", finalTarget.getName()));
          }
 
-         boolean var14 = !var12.equals(var11);
-         this.summonGuard(var7, var5, var13, var8, var9, var11, var14);
+         boolean attack = !finalTarget.equals(otherEntity);
+         this.summonGuard(entityType, isBaby, name, liveTicks, amount, otherEntity, attack);
          return true;
       }
    }
 
-   public boolean isGuard(Entity var1) {
-      return this.guards.containsKey(var1.getUniqueId());
+   public boolean isGuard(Entity entity) {
+      return this.guards.containsKey(entity.getUniqueId());
    }
 
-   public void steal(UUID var1, UUID var2) {
-      if (this.guards.containsValue(var1)) {
-         HashMap var3 = new HashMap<>(this.guards);
-
-         for (Entry var5 : this.getGuards().entrySet()) {
-            if (Objects.equals(var5.getValue(), var1)) {
-               var3.remove(var5.getKey(), var1);
-               var3.put((UUID)var5.getKey(), var2);
-               Entity var6 = Bukkit.getEntity((UUID)var5.getKey());
-               if (var6 instanceof Creature) {
-                  ((Creature)var6).setTarget(Bukkit.getPlayer(var2));
+   public void steal(UUID oldOwner, UUID newOwner) {
+      if (this.guards.containsValue(oldOwner)) {
+         Map<UUID, UUID> newMap = new HashMap<>(this.guards);
+
+         for (Entry<UUID, UUID> entry : this.getGuards().entrySet()) {
+            if (Objects.equals(entry.getValue(), oldOwner)) {
+               newMap.remove(entry.getKey(), oldOwner);
+               newMap.put(entry.getKey(), newOwner);
+               Entity entity = Bukkit.getEntity(entry.getKey());
+               if (entity instanceof Creature) {
+                  ((Creature)entity).setTarget(Bukkit.getPlayer(newOwner));
                }
             }
          }
 
-         this.guards.putAll(var3);
+         this.guards.putAll(newMap);
       }
    }
 
-   public void summonGuard(EntityType var1, boolean var2, String var3, int var4, int var5, LivingEntity var6, boolean var7) {
-      for (int var8 = 0; var8 < var5; var8++) {
-         this.summonGuard(var1, var2, var3, var4, var6, var7);
+   public void summonGuard(EntityType guardType, boolean isBaby, String name, int liveTicks, int amount, LivingEntity target, boolean attack) {
+      for (int i = 0; i < amount; i++) {
+         this.summonGuard(guardType, isBaby, name, liveTicks, target, attack);
       }
    }
 
-   public void summonGuard(EntityType var1, boolean var2, String var3, int var4, LivingEntity var5, boolean var6) {
-      Entity var7 = EntitySpawnUtils.spawnEntity(this.getPlugin(), var5.getWorld(), var5.getLocation(), var1);
-      if (var3 != null && !var3.isEmpty()) {
-         var7.setCustomName(var3);
-         var7.setCustomNameVisible(true);
+   public void summonGuard(EntityType guardType, boolean isBaby, String name, int liveTicks, LivingEntity target, boolean attack) {
+      Entity entity = EntitySpawnUtils.spawnEntity(this.getPlugin(), target.getWorld(), target.getLocation(), guardType);
+      if (name != null && !name.isEmpty()) {
+         entity.setCustomName(name);
+         entity.setCustomNameVisible(true);
       }
 
-      if (var7 instanceof LivingEntity var8) {
-         var8.setCanPickupItems(false);
-         var8.setRemoveWhenFarAway(true);
-         var8.addPotionEffect(new PotionEffect(PotionEffectType.INCREASE_DAMAGE, var4, 1, false, true));
+      if (entity instanceof LivingEntity le) {
+         le.setCanPickupItems(false);
+         le.setRemoveWhenFarAway(true);
+         le.addPotionEffect(new PotionEffect(PotionEffectType.STRENGTH, liveTicks, 1, false, true));
       }
 
-      this.guards.put(var7.getUniqueId(), var5.getUniqueId());
-      if (var7 instanceof Creature) {
-         ((Creature)var7).setTarget(var5);
+      this.guards.put(entity.getUniqueId(), target.getUniqueId());
+      if (entity instanceof Creature) {
+         ((Creature)entity).setTarget(target);
       }
 
-      if (var2 && var7 instanceof Ageable) {
-         ((Ageable)var7).setBaby();
+      if (isBaby && entity instanceof Ageable) {
+         ((Ageable)entity).setBaby();
       }
 
-      Bukkit.getScheduler().runTaskLater(EffectsHandler.getInstance(), () -> {
-         var7.remove();
-         if (!var7.isDead() && var7 instanceof LivingEntity) {
-            ((LivingEntity)var7).damage(2.147483647E9);
+      entity.getScheduler().runDelayed(this.getPlugin(), t -> {
+         entity.remove();
+         if (!entity.isDead() && entity instanceof LivingEntity) {
+            ((LivingEntity)entity).damage(2.147483647E9);
          }
-      }, var4);
+      }, null, (long)liveTicks);
    }
 
    @EventHandler(priority = EventPriority.HIGHEST, ignoreCancelled = true)
-   public void onExplode(ExplosionPrimeEvent var1) {
-      if (this.isGuard(var1.getEntity())) {
-         if (var1.getEntity() instanceof LivingEntity var2) {
-            var2.getActivePotionEffects().forEach(var1x -> var2.removePotionEffect(var1x.getType()));
+   public void onExplode(ExplosionPrimeEvent e) {
+      if (this.isGuard(e.getEntity())) {
+         if (e.getEntity() instanceof LivingEntity entity) {
+            entity.getActivePotionEffects().forEach(pe -> entity.removePotionEffect(pe.getType()));
          }
       }
    }
 
    @EventHandler(priority = EventPriority.HIGHEST, ignoreCancelled = true)
-   public void onGuardDeath(EntityDeathEvent var1) {
-      if (this.isGuard(var1.getEntity())) {
-         var1.setDroppedExp(0);
-         var1.getDrops().clear();
+   public void onGuardDeath(EntityDeathEvent e) {
+      if (this.isGuard(e.getEntity())) {
+         e.setDroppedExp(0);
+         e.getDrops().clear();
       }
    }
 
    @EventHandler(priority = EventPriority.HIGHEST, ignoreCancelled = true)
-   private void entityTargetEvent(EntityTargetEvent var1) {
-      if (this.isGuard(var1.getEntity())) {
-         if (var1.getTarget() instanceof LivingEntity var2) {
-            if (!var2.getUniqueId().equals(this.guards.get(var1.getEntity().getUniqueId()))) {
-               var1.setCancelled(true);
-               LivingEntity var10 = null;
-               double var4 = 32.0;
-
-               for (Entity var7 : var1.getTarget().getNearbyEntities(var4, var4, var4)) {
-                  if (var7 instanceof LivingEntity && !var7.getUniqueId().equals(var7.getUniqueId())) {
-                     double var8 = var7.getLocation().distance(var2.getLocation());
-                     if (var8 < var4) {
-                        var10 = (LivingEntity)var7;
-                        var4 = var8;
+   private void entityTargetEvent(EntityTargetEvent e) {
+      if (this.isGuard(e.getEntity())) {
+         if (e.getTarget() instanceof LivingEntity target) {
+            if (!target.getUniqueId().equals(this.guards.get(e.getEntity().getUniqueId()))) {
+               e.setCancelled(true);
+               LivingEntity closest = null;
+               double distance = 32.0;
+
+               for (Entity entity : e.getTarget().getNearbyEntities(distance, distance, distance)) {
+                  if (entity instanceof LivingEntity && !entity.getUniqueId().equals(entity.getUniqueId())) {
+                     double entitiesDistance = entity.getLocation().distance(target.getLocation());
+                     if (entitiesDistance < distance) {
+                        closest = (LivingEntity)entity;
+                        distance = entitiesDistance;
                      }
                   }
                }
 
-               if (var10 != null) {
-                  var1.setTarget(var10);
+               if (closest != null) {
+                  e.setTarget(closest);
                }
             }
          }
@@ -190,18 +190,16 @@ public class GuardEffect extends AdvancedEffect {
    }
 
    @EventHandler(priority = EventPriority.MONITOR)
-   public void onSpawn(CreatureSpawnEvent var1) {
-      if (this.isGuard(var1.getEntity())) {
-         var1.setCancelled(false);
+   public void onSpawn(CreatureSpawnEvent e) {
+      if (this.isGuard(e.getEntity())) {
+         e.setCancelled(false);
       }
    }
 
-   @Generated
    public Map<UUID, UUID> getGuards() {
       return this.guards;
    }
 
-   @Generated
    public static GuardEffect getGuardEffect() {
       return guardEffect;
    }

