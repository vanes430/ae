From: Folia Compatibility Patch
Subject: [PATCH 0004] Internal-Effects: Migrate potion, boost, extinguish, guard, and teleport effects to SchedulerUtils/FoliaScheduler

AE PATCH REASON: Standard enchantment effects use legacy scheduler
AE PATCH FIX: Migrate potion, boost, extinguish, guard, and teleport effects to SchedulerUtils/FoliaScheduler
AE PATCH RISK: MEDIUM
AE PATCH EXCLUDES: net/advancedplugins/ae/impl/effects/effects/effects/internal/ApplyPotionEffect.class, net/advancedplugins/ae/impl/effects/effects/effects/internal/BoostEffect.class, net/advancedplugins/ae/impl/effects/effects/effects/internal/ExtinguishEffect.class, net/advancedplugins/ae/impl/effects/effects/effects/internal/GuardEffect.class, net/advancedplugins/ae/impl/effects/effects/effects/internal/TeleportBehindEffect.class

---
diff --git a/net/advancedplugins/ae/impl/effects/effects/effects/internal/ApplyPotionEffect.java b/net/advancedplugins/ae/impl/effects/effects/effects/internal/ApplyPotionEffect.java
index e1c6894..095e2e8 100644
--- a/net/advancedplugins/ae/impl/effects/effects/effects/internal/ApplyPotionEffect.java
+++ b/net/advancedplugins/ae/impl/effects/effects/effects/internal/ApplyPotionEffect.java
@@ -12,9 +12,9 @@ import net.advancedplugins.ae.impl.effects.effects.actions.execution.ExecutionTa
 import net.advancedplugins.ae.impl.effects.effects.effects.AdvancedEffect;
 import net.advancedplugins.ae.impl.utils.ASManager;
 import net.advancedplugins.ae.impl.utils.PotionEffectMatcher;
-import net.advancedplugins.ae.impl.utils.SchedulerUtils;
 import net.advancedplugins.ae.impl.utils.nbt.utils.MinecraftVersion;
 import org.apache.commons.lang3.tuple.Triple;
+import org.bukkit.Bukkit;
 import org.bukkit.entity.LivingEntity;
 import org.bukkit.plugin.java.JavaPlugin;
 import org.bukkit.potion.PotionEffect;
@@ -105,7 +105,7 @@ public class ApplyPotionEffect extends AdvancedEffect {
    }
 
    static {
-      SchedulerUtils.runTaskTimer(() -> {
+      Bukkit.getGlobalRegionScheduler().runAtFixedRate(ASManager.getInstance(), task -> {
          long currentTime = System.currentTimeMillis();
 
          while (!healthQueue.isEmpty() && healthQueue.peek().getLeft() + 500L < currentTime) {
@@ -113,9 +113,11 @@ public class ApplyPotionEffect extends AdvancedEffect {
             if (triple != null) {
                LivingEntity target = (LivingEntity)triple.getMiddle();
                double hpBefore = (Double)triple.getRight();
-               if (!(hpBefore <= target.getHealth())) {
-                  target.setHealth(Math.min(hpBefore, target.getMaxHealth()));
-               }
+               target.getScheduler().run(ASManager.getInstance(), t -> {
+                  if (!target.isDead() && hpBefore > target.getHealth()) {
+                     target.setHealth(Math.min(hpBefore, target.getMaxHealth()));
+                  }
+               }, null);
             }
          }
       }, 1L, 1L);

diff --git a/net/advancedplugins/ae/impl/effects/effects/effects/internal/BoostEffect.java b/net/advancedplugins/ae/impl/effects/effects/effects/internal/BoostEffect.java
index 62065e4..d0c13a1 100644
--- a/net/advancedplugins/ae/impl/effects/effects/effects/internal/BoostEffect.java
+++ b/net/advancedplugins/ae/impl/effects/effects/effects/internal/BoostEffect.java
@@ -27,10 +27,7 @@ public class BoostEffect extends AdvancedEffect {
    }
 
    public boolean executeEffect(ExecutionTask task, LivingEntity target, String[] args) {
-      Bukkit.getScheduler()
-         .runTaskLater(
-            EffectsHandler.getInstance(),
-            () -> {
+      target.getScheduler().runDelayed(ASManager.getInstance(), t -> {
                try {
                   String dir = "UP";
                   double amount = 1.0;
@@ -95,9 +92,7 @@ public class BoostEffect extends AdvancedEffect {
                } catch (Exception var23) {
                   var23.printStackTrace();
                }
-            },
-            1L
-         );
+            }, null, 1L);
       return true;
    }
 

diff --git a/net/advancedplugins/ae/impl/effects/effects/effects/internal/ExtinguishEffect.java b/net/advancedplugins/ae/impl/effects/effects/effects/internal/ExtinguishEffect.java
index 38a20dd..d1f39fb 100644
--- a/net/advancedplugins/ae/impl/effects/effects/effects/internal/ExtinguishEffect.java
+++ b/net/advancedplugins/ae/impl/effects/effects/effects/internal/ExtinguishEffect.java
@@ -2,7 +2,7 @@ package net.advancedplugins.ae.impl.effects.effects.effects.internal;
 
 import net.advancedplugins.ae.impl.effects.effects.actions.execution.ExecutionTask;
 import net.advancedplugins.ae.impl.effects.effects.effects.AdvancedEffect;
-import net.advancedplugins.ae.impl.utils.SchedulerUtils;
+import net.advancedplugins.ae.impl.utils.ASManager;
 import org.bukkit.entity.LivingEntity;
 import org.bukkit.plugin.java.JavaPlugin;
 
@@ -12,7 +12,7 @@ public class ExtinguishEffect extends AdvancedEffect {
    }
 
    public boolean executeEffect(ExecutionTask task, LivingEntity target, String[] args) {
-      SchedulerUtils.runTaskLater(() -> target.setFireTicks(0), 1L);
+      target.getScheduler().runDelayed(ASManager.getInstance(), t -> target.setFireTicks(0), null, 1L);
       return true;
    }
 }

diff --git a/net/advancedplugins/ae/impl/effects/effects/effects/internal/GuardEffect.java b/net/advancedplugins/ae/impl/effects/effects/effects/internal/GuardEffect.java
index 0d6526c..9dfd6b8 100644
--- a/net/advancedplugins/ae/impl/effects/effects/effects/internal/GuardEffect.java
+++ b/net/advancedplugins/ae/impl/effects/effects/effects/internal/GuardEffect.java
@@ -124,7 +124,7 @@ public class GuardEffect extends AdvancedEffect {
       if (entity instanceof LivingEntity le) {
          le.setCanPickupItems(false);
          le.setRemoveWhenFarAway(true);
-         le.addPotionEffect(new PotionEffect(PotionEffectType.INCREASE_DAMAGE, liveTicks, 1, false, true));
+         le.addPotionEffect(new PotionEffect(PotionEffectType.STRENGTH, liveTicks, 1, false, true));
       }
 
       this.guards.put(entity.getUniqueId(), target.getUniqueId());
@@ -136,12 +136,12 @@ public class GuardEffect extends AdvancedEffect {
          ((Ageable)entity).setBaby();
       }
 
-      Bukkit.getScheduler().runTaskLater(EffectsHandler.getInstance(), () -> {
+      entity.getScheduler().runDelayed(this.getPlugin(), t -> {
          entity.remove();
          if (!entity.isDead() && entity instanceof LivingEntity) {
             ((LivingEntity)entity).damage(2.147483647E9);
          }
-      }, liveTicks);
+      }, null, (long)liveTicks);
    }
 
    @EventHandler(priority = EventPriority.HIGHEST, ignoreCancelled = true)

diff --git a/net/advancedplugins/ae/impl/effects/effects/effects/internal/TeleportBehindEffect.java b/net/advancedplugins/ae/impl/effects/effects/effects/internal/TeleportBehindEffect.java
index ac3444b..d65a523 100644
--- a/net/advancedplugins/ae/impl/effects/effects/effects/internal/TeleportBehindEffect.java
+++ b/net/advancedplugins/ae/impl/effects/effects/effects/internal/TeleportBehindEffect.java
@@ -27,7 +27,7 @@ public class TeleportBehindEffect extends AdvancedEffect {
       if (!ASManager.isAir(to.getBlock())) {
          return true;
       } else {
-         target.teleport(to, TeleportCause.UNKNOWN);
+         target.teleportAsync(to, TeleportCause.UNKNOWN);
          return true;
       }
    }

