From: Shimazu <vanessimbolon2020@gmail.com>
Date: Fri, 31 Jul 2026 02:19:04 +0000
Subject: DamageHandler: Remove BukkitRunnable, migrate revive to EntityScheduler

AE PATCH REASON: BukkitRunnable is incompatible with Folia server architecture; obfuscated variable names reduce maintainability.
AE PATCH FIX: Remove BukkitRunnable import. Replace BukkitRunnable in revive() with player.getScheduler().runDelayed(). Use teleportAsync() instead of teleport(). Rename var1/var2/var3 to descriptive parameter names.
AE PATCH RISK: LOW

---
diff --git a/net/advancedplugins/ae/impl/effects/effects/actions/handlers/DamageHandler.java b/net/advancedplugins/ae/impl/effects/effects/actions/handlers/DamageHandler.java
--- a/net/advancedplugins/ae/impl/effects/effects/actions/handlers/DamageHandler.java	2026-07-30 10:27:31.669350132 +0000
+++ b/net/advancedplugins/ae/impl/effects/effects/actions/handlers/DamageHandler.java	2026-07-30 10:28:05.846658248 +0000
@@ -11,36 +11,35 @@
 import org.bukkit.event.entity.EntityDamageEvent;
 import org.bukkit.event.entity.EntityDamageEvent.DamageCause;
 import org.bukkit.metadata.FixedMetadataValue;
-import org.bukkit.scheduler.BukkitRunnable;
 
 public class DamageHandler {
-   public void damage(EntityDamageEvent var1, double var2, boolean var4) {
-      if (var1.getEntity() instanceof Damageable) {
-         Entity var5 = var1 instanceof EntityDamageByEntityEvent ? ((EntityDamageByEntityEvent)var1).getDamager() : null;
-         this.damage((Damageable)var1.getEntity(), var5, var2);
+   public void damage(EntityDamageEvent event, double damage, boolean cancelled) {
+      if (event.getEntity() instanceof Damageable) {
+         Entity damager = event instanceof EntityDamageByEntityEvent ? ((EntityDamageByEntityEvent)event).getDamager() : null;
+         this.damage((Damageable)event.getEntity(), damager, damage);
       }
    }
 
-   public boolean damageIgnoringArmor(Damageable var1, LivingEntity var2, LivingEntity var3, double var4) {
-      double var6 = var1.getAbsorptionAmount();
-      if (var6 > 0.0) {
-         double var8 = var6 - var4;
-         if (var8 >= 0.0) {
-            var1.setAbsorptionAmount(var8);
+   public boolean damageIgnoringArmor(Damageable damaged, LivingEntity damager, LivingEntity target, double damage) {
+      double absorption = damaged.getAbsorptionAmount();
+      if (absorption > 0.0) {
+         double remainingAbsorption = absorption - damage;
+         if (remainingAbsorption >= 0.0) {
+            damaged.setAbsorptionAmount(remainingAbsorption);
             return false;
          }
 
-         var4 -= var6;
-         var1.setAbsorptionAmount(0.0);
+         damage -= absorption;
+         damaged.setAbsorptionAmount(0.0);
       }
 
-      if (!var1.isDead() && !(var1.getHealth() <= 0.0)) {
-         double var10 = var2.getHealth() - var4;
-         if (var10 < 0.0) {
-            var1.setHealth(0.0);
+      if (!damaged.isDead() && !(damaged.getHealth() <= 0.0)) {
+         double newHealth = damager.getHealth() - damage;
+         if (newHealth < 0.0) {
+            damaged.setHealth(0.0);
             return true;
          } else {
-            var1.setHealth(var10);
+            damaged.setHealth(newHealth);
             return false;
          }
       } else {
@@ -48,44 +47,42 @@
       }
    }
 
-   public void damage(Damageable var1, double var2) {
-      this.damage(var1, null, var2);
+   public void damage(Damageable damaged, double damage) {
+      this.damage(damaged, null, damage);
    }
 
-   public void damage(Damageable var1, Entity var2, double var3) {
-      this.damage(var1, var2, var3, DamageCause.CUSTOM);
+   public void damage(Damageable damaged, Entity damager, double damage) {
+      this.damage(damaged, damager, damage, DamageCause.CUSTOM);
    }
 
-   public void damage(Damageable var1, Entity var2, double var3, DamageCause var5) {
-      if (!var1.isDead() && !(var1.getHealth() <= 0.0)) {
-         var1.setMetadata("ae_ignore", new FixedMetadataValue(EffectsHandler.getInstance(), true));
-         var3 = Math.abs(var3);
-         var1.damage(var3, var2);
-         var1.removeMetadata("ae_ignore", EffectsHandler.getInstance());
+   public void damage(Damageable damaged, Entity damager, double damage, DamageCause cause) {
+      if (!damaged.isDead() && !(damaged.getHealth() <= 0.0)) {
+         damaged.setMetadata("ae_ignore", new FixedMetadataValue(EffectsHandler.getInstance(), true));
+         damage = Math.abs(damage);
+         damaged.damage(damage, damager);
+         damaged.removeMetadata("ae_ignore", EffectsHandler.getInstance());
       }
    }
 
-   public void heal(Entity var1, double var2) {
-      Damageable var4 = (Damageable)var1;
-      if (!var4.isDead() && !(var4.getHealth() <= 0.0)) {
-         double var5 = var4.getHealth();
-         double var7 = Math.abs(var2);
-         double var9 = MathUtils.clamp(var5 + var7, 0.0, var4.getMaxHealth());
-         var4.setHealth(var9);
+   public void heal(Entity entity, double amount) {
+      Damageable damageable = (Damageable)entity;
+      if (!damageable.isDead() && !(damageable.getHealth() <= 0.0)) {
+         double currentHealth = damageable.getHealth();
+         double healAmount = Math.abs(amount);
+         double newHealth = MathUtils.clamp(currentHealth + healAmount, 0.0, damageable.getMaxHealth());
+         damageable.setHealth(newHealth);
       }
    }
 
-   public void revive(Entity var1, final Location var2) {
-      Damageable var3 = (Damageable)var1;
-      if (var1 instanceof Player) {
-         if (var3.isDead() || var3.getHealth() <= 0.0) {
-            final Player var4 = (Player)var1;
-            var4.spigot().respawn();
-            (new BukkitRunnable() {
-               public void run() {
-                  var4.teleport(var2);
-               }
-            }).runTaskLater(EffectsHandler.getInstance(), 1L);
+   public void revive(Entity entity, Location location) {
+      Damageable damageable = (Damageable)entity;
+      if (entity instanceof Player) {
+         if (damageable.isDead() || damageable.getHealth() <= 0.0) {
+            Player player = (Player)entity;
+            player.spigot().respawn();
+            player.getScheduler().runDelayed(EffectsHandler.getInstance(), task -> {
+               player.teleportAsync(location);
+            }, null, 1L);
          }
       }
    }
